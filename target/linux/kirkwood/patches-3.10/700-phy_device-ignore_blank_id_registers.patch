Index: linux-3.10.44/drivers/net/phy/phy_device.c
===================================================================
--- linux-3.10.44.orig/drivers/net/phy/phy_device.c
+++ linux-3.10.44/drivers/net/phy/phy_device.c
@@ -340,7 +340,8 @@ struct phy_device *get_phy_device(struct
 
 	/* If the phy_id is mostly Fs, there is no device there */
 	if ((phy_id & 0x1fffffff) == 0x1fffffff)
-		return NULL;
+		pr_info("get_phy_device: phy_id mostly f's, but ignoring\n");
+		/* return NULL; */
 
 	dev = phy_device_create(bus, addr, phy_id, is_c45, &c45_ids);
 
